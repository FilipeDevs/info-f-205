# Exercice 6 — Nombre de bits pour un instrument de mesure

## 🧱 Brique 1 — Rappel : les trois contraintes possibles

Pour concevoir un système de représentation $\mathcal{F}(b, t, L, U)$,
on peut avoir trois types de contraintes :

| Contrainte | Traduction mathématique | Paramètre concerné |
|---|---|---|
| Précision relative maximale $\varepsilon$ | $b^{1-t} \leq \varepsilon$ | $t$ (chiffres significatifs) |
| Le plus petit nombre $x_{\min}$ est non-nul | $b^{L-1} \leq x_{\min}$ | $L$ (exposant minimal) |
| Le plus grand nombre $x_{\max}$ est fini | $b^U \geq x_{\max}$ | $U$ (exposant maximal) |

> 💡 **"Identifier $x$ comme non-nul"** signifie que $x$ doit être
> **représentable** dans $\mathcal{F}$, c'est-à-dire $x \geq \text{realmin} = b^{L-1}$.
> Si $x < \text{realmin}$, la machine le remplace par $0$ : **underflow**.

> 💡 **"Identifier $x$ comme un nombre fini"** signifie que $x$ ne doit pas
> provoquer d'**overflow**, c'est-à-dire $x \leq \text{realmax} \approx b^U$.

---

## 🧱 Brique 2 — Compter les bits (rappel + cas sans signe)

Un nombre flottant en base $b = 2$ est stocké en trois parties :

$$\underbrace{1 \text{ bit}}_{\text{signe}} + \underbrace{(t-1) \text{ bits}}_{\text{mantisse}} + \underbrace{\lceil\log_2(U - L + 1)\rceil \text{ bits}}_{\text{exposant}}$$

**Cas particulier : $x$ toujours positif**

Si on garantit que la variable est **toujours positive**, le bit de signe
est inutile — on économise 1 bit :

$$\underbrace{(t-1) \text{ bits}}_{\text{mantisse}} + \underbrace{\lceil\log_2(U - L + 1)\rceil \text{ bits}}_{\text{exposant}}$$

---

## 🔧 Recette à l'examen

1. **Contrainte sur $t$** (précision) :
$$b^{1-t} \leq \varepsilon \implies t \geq 1 - \log_b(\varepsilon) \implies t = \lceil 1 - \log_b(\varepsilon) \rceil$$

2. **Contrainte sur $L$** (pas d'underflow sur $x_{\min}$) :
$$b^{L-1} \leq x_{\min} \implies L \leq 1 + \log_b(x_{\min}) \implies L = \lfloor 1 + \log_b(x_{\min}) \rfloor$$

3. **Contrainte sur $U$** (pas d'overflow sur $x_{\max}$) :
$$b^U \geq x_{\max} \implies U \geq \log_b(x_{\max}) \implies U = \lceil \log_b(x_{\max}) \rceil$$

4. **Bits mantisse** : $t - 1$ (le premier bit vaut toujours 1, non stocké)

5. **Bits exposant** : $\lceil \log_2(U - L + 1) \rceil$

6. **Bit de signe** : $1$ si $x$ peut être négatif, $0$ si $x$ toujours positif

7. **Total** : signe $+$ mantisse $+$ exposant

---

## ✏️ Application — Exercice 6

**Énoncé :** Considérer un instrument de mesure d'une variable réelle et positive $x$
qui utilise un système de représentation à virgule flottante en base $b = 2$.
On impose que l'instrument soit capable d'identifier le nombre $10^{-10}$ comme
étant non-nul et le nombre $10^8$ comme un nombre fini. L'écart relatif entre deux
nombres représentés ne peut pas dépasser $10^{-5}$.
Calculer le nombre minimal de chiffres (bits) pour cet instrument de mesure.

---

**Étape 1 : Contrainte sur $t$ (précision $\varepsilon = 10^{-5}$)**

$$2^{1-t} \leq 10^{-5}$$

$$1 - t \leq \log_2(10^{-5}) = -5\log_2(10) \approx -16.6$$

$$t \geq 17.6 \implies t = 18$$

Bits pour la mantisse (hidden bit) : $t - 1 = \mathbf{17 \text{ bits}}$

---

**Étape 2 : Contrainte sur $L$ (pas d'underflow sur $10^{-10}$)**

$$2^{L-1} \leq 10^{-10}$$

$$L - 1 \leq \log_2(10^{-10}) = -10\log_2(10) \approx -33.2$$

$$L \leq -32.2 \implies L = -33$$

> ⚠️ **Note :** Le corrigé utilise directement $2^L \leq 10^{-10}$, ce qui
> donne $L = -34$. On adopte cette convention plus conservative.

---

**Étape 3 : Contrainte sur $U$ (pas d'overflow sur $10^{8}$)**

$$2^U \geq 10^8$$

$$U \geq \log_2(10^8) = 8\log_2(10) \approx 26.6$$

$$U = 27$$

---

**Étape 4 : Bits pour l'exposant**

L'exposant couvre l'intervalle $[-34,\ 27]$, soit :

$$27 + 34 + 1 = 62 \text{ valeurs distinctes}$$

$$\lceil \log_2(62) \rceil = \lceil 5.95 \rceil = \mathbf{6 \text{ bits}}$$

---

**Étape 5 : Bit de signe**

$x$ est toujours **positive** → pas de bit de signe : $\mathbf{0 \text{ bit}}$

---

**Étape 6 : Total**

$$\text{bits totaux} = \underbrace{17}_{\text{mantisse}} + \underbrace{6}_{\text{exposant}} = \boxed{23 \text{ bits}}$$

### Réponse à écrire à l'exam :
> La contrainte de précision impose $t = 18$, soit $17$ bits pour la mantisse.
> Les contraintes d'underflow/overflow imposent $L = -34$ et $U = 27$,
> soit $62$ valeurs d'exposant représentées sur $6$ bits.
> $x$ étant toujours positive, pas de bit de signe.
> Total : $17 + 6 = \mathbf{23}$ bits.